package com.smarthome.devices.version1;

import com.smarthome.devices.Camera;


// Version1 variant of Camera.
public class Version1Camera extends Camera {
    public Version1Camera(String id, String name) {
        super(id, name);
    }
}
